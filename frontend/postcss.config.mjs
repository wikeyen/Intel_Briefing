// ABOUTME: PostCSS config enabling Tailwind CSS processing.
// ABOUTME: Uses @tailwindcss/postcss for Next.js compatibility.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
export default config
